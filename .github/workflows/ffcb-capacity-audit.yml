name: FFCB capacity audit
on:
  pull_request:
    branches: [master]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install requests
        run: python -m pip install --disable-pip-version-check requests
      - name: Run inventory-backed dominance RFQ lock audit
        run: python research/ibdr_lock_ci.py
      - name: Upload result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: research-results
          path: ibdr_lock.json
          if-no-files-found: ignore
